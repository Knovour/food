.dimmer {
  position: fixed 0;
  background: rgba(53, 53, 53, .5);
  width: 100vw;
  height: 100vh;
  z-index: 100;

  transition: all 200ms linear;

  &._hide {
    opacity: 0;
    visibility: hidden;
  }

  &._show {
    opacity: 1;
    visibility: visible;
  }
}